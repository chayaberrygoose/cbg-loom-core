# Ignore secret material
.env
.venv/

# Ignore Python cache artifacts
**/__pycache__/
*.py[cod]

# Ignore runtime logs
*.log
artifacts/generated/**/*.log
artifacts/specimens/

# Images under artifacts/ are tracked via Git LFS (see .gitattributes).
# Non-image files in artifacts/catalog/ (JSON, Markdown) are tracked normally.
